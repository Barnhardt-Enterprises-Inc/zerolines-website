# Docs

Reference docs for behavior the code doesn't explain. Read the one that matches the code you're about to change; each line below says when.

- `deploy.md` — environments, credentials, deploy and rollback steps, smoke-test URLs. Read by `/deploy`.
